---
const navigationItems = ["Posts"];
---

<header>
  <div class="flex justify-between items-center sm:w-full">
    <div class="text-2xl text-bold">
      <a href="/">
        <h1>Woong.log</h1>
      </a>
    </div>
    <nav>
      <ul class="flex">
        {
          navigationItems.map((item) => (
            <a href={`/${item.toLowerCase()}`}>
              <div class={`px-3 py-5`}>
                <li>{item}</li>
              </div>
            </a>
          ))
        }
      </ul>
    </nav>
  </div>
</header>
